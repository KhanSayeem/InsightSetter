export const PRIMARY_NAV_CATEGORY_LIMIT = 8;

export const PRIMARY_NAV_CATEGORY_HELP = `You can feature up to ${PRIMARY_NAV_CATEGORY_LIMIT} categories directly in the navigation bar. The rest are listed inside “More.”`;
